package org.cyrilselyanin.bustvs.repository;

import org.cyrilselyanin.bustvs.domain.BusPointType;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BusPointTypeRepository extends JpaRepository<BusPointType, Long> {
}
